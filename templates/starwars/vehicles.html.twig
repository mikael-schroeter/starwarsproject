{% extends 'base.html.twig' %}

{% block title %}Starwars Project{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Starwars vehicle details!</h1>

    {{ content['name'] }}
    <ul>
        {% for key, value in content[1:] %}
            {% if value is iterable  %}
                <li> {{ key }}
                    <ul>
                        {% for index, value in value %}
                            {% if key == "films"  %}
                                <li><a href="http://localhost:8080/starwars/films/{{ value|split('/')[5] }}">{{ index }}: {{ value }}</a></li>
                            {% else %} 
                                <li>{{ index }}: {{ value }}</li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </li>    
            {% else %}    
                <li>{{ key }}: {{ value }}</li>
            {% endif %}
        {% endfor %}
    </ul>
    <a href="javascript:history.back()">
        <button type="button" class="btn btn-primary">Back</button>
    </a>
    <a href="http://localhost:8080/starwars/planets">
        <button type="button" class="btn btn-primary">back to all planets</button>
    </a>
</div>
{% endblock %}
